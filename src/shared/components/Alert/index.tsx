// import { AlertProps } from 'antd';
// //
// import { StyledAlert } from '@/shared/theme/overrides/Alert';
// import { Close } from '@/shared/assets/icons';

// export const Alert = (props: AlertProps) => (
//   <StyledAlert {...props} closeIcon={props.closeIcon || <Close />} />
// );
